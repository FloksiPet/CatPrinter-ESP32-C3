# GB01 Cat Printer - BLE Protocol Documentation

## Огляд

GB01 Cat Printer комунікує через Bluetooth Low Energy (BLE) за власним протоколом, оберненим інженерами з APK оригіналу додатка.

## Архітектура BLE

### Service UUID
```
0000ae30-0000-1000-8000-00805f9b34fb
```

### Характеристики

| UUID | Назва | Тип | Опис |
|------|-------|-----|------|
| `0000ae01-0000-1000-8000-00805f9b34fb` | Write | Write/NoResponse | Відправка команд на принтер |
| `0000ae02-0000-1000-8000-00805f9b34fb` | Notify | Notify | Отримання відповідей від принтера |

## Формат пакету

### Загальна структура

```
┌────────┬────────┬─────┬────────┬─────┬────────┬──────────┬────────┬────────┐
│ 0x51   │ 0x78   │ CMD │ 0x00   │ LEN │ 0x00   │ DATA[*]  │ CRC8   │ 0xFF   │
└────────┴────────┴─────┴────────┴─────┴────────┴──────────┴────────┴────────┘
  Magic    Magic   Cmd   Resv    Len   Resv     Payload    Checksum  End
  1 byte   1 byte  1 b   1 byte  1 b   1 byte   LEN bytes  1 byte   1 byte
```

### Поле за полем

#### Magic Number (0x51 0x78)
- Завжди одинакові для всіх пакетів
- Дозволяє принтеру розпізнати коректний пакет

#### CMD (1 байт)
- Команда для виконання
- Див. таблицю команд нижче

#### Резервні поля (0x00)
- Завжди 0x00
- Можливо, зарезервовані для майбутнього розширення

#### LEN (1 байт)
- Кількість байт у полі DATA
- Максимум: 255 байт
- Якщо LEN=0, то DATA відсутній

#### CRC8 (1 байт)
- Контрольна сума по CRC8 для поля DATA
- Алгоритм: див. CRC8_TABLE
- Якщо LEN=0 (немає даних), то CRC8=0x00

#### End Marker (0xFF)
- Маркер кінця пакету
- Завжди 0xFF

## Алгоритм CRC8

```cpp
uint8_t crc8(uint8_t* data, uint8_t len) {
    uint8_t crc = 0;
    for (uint8_t i = 0; i < len; i++)
        crc = CRC8_TABLE[(crc ^ data[i]) & 0xFF];
    return crc;
}
```

Табліця CRC8 міститься у файлі коду.

## Таблиця команд

### 0xA0 - RetractPaper (Втягнути папір)

**Опис**: Втягти папір назад на вказану кількість кроків

**Data Format**:
```
Byte 0: HIGH byte (кількість кроків / 256)
Byte 1: LOW byte (кількість кроків % 256)
```

**Приклад**: Втягти на 100 кроків
```
DATA = [0x00, 0x64]  // 100 = 0x0064
```

**Response**: Немає (Write Without Response)

---

### 0xA1 - FeedPaper (Витягнути папір)

**Опис**: Витягти папір вперед на вказану кількість кроків

**Data Format**:
```
Byte 0: HIGH byte (кількість кроків / 256)
Byte 1: LOW byte (кількість кроків % 256)
```

**Приклади**:
- Витягти на 1 крок:
  ```
  DATA = [0x00, 0x01]
  ```
- Витягти на 30 кроків:
  ```
  DATA = [0x00, 0x1E]  // 30 = 0x001E
  ```
- Витягти на 112 кроків (видалення паперу):
  ```
  DATA = [0x00, 0x70]  // 112 = 0x0070
  ```

**Response**: Немає

---

### 0xA2 - DrawBitmap (Намалювати рядок)

**Опис**: Надрукувати один рядок зображення

**Data Format**:
```
50 байт = 1 рядок
400 пікселів = 50 байт * 8 біт/байт
1 біт = 1 піксель
  0 = білий (не друкувати)
  1 = чорний (друкувати)
```

**Макет даних**:
```
┌──────────────────────────────────────────────────────────┐
│ Byte 0   │ Byte 1   │ ... │ Byte 49  │
│ 8 px (0) │ 8 px (1) │ ... │ 8 px(49) │
└──────────────────────────────────────────────────────────┘
  Pixels    Pixels         Pixels
  0-7       8-15           392-399
```

**Бітовий порядок в байті**:
```
Bit 7  Bit 6  Bit 5  Bit 4  Bit 3  Bit 2  Bit 1  Bit 0
 px0   px1    px2    px3    px4    px5    px6    px7
```

**Приклад**: Чорна лінія (всі пікселі)
```
DATA = [0xFF, 0xFF, ..., 0xFF]  // 50x 0xFF
```

**Приклад**: Чередуються пікселі
```
DATA = [0xAA, 0xAA, ..., 0xAA]  // 50x 0xAA = 10101010
```

**Response**: Немає

---

### 0xA3 - GetStatus (Отримати статус)

**Опис**: Запитати поточний статус принтера

**Data Format**:
```
Жодних даних (LEN = 0)
```

**Response**:
```
DATA = [status_byte]
```

**Статус-байт (біти)**:
| Біт | Маска | Опис |
|-----|-------|------|
| 0 | 0x01 | Папір не встановлений |
| 1 | 0x02 | Кришка відкрита |
| 2 | 0x04 | Магнітна головка гаряча |
| 3 | 0x08 | Низький рівень батареї |
| 4-6 | 0x70 | Не використовується |
| 7 | 0x80 | Принтер готовий / Немає помилок |

**Приклад**: Принтер готовий
```
Status = 0x80
```

---

### 0xA4 - SetQuality (Встановити якість друку)

**Опис**: Встановити якість друку

**Data Format**:
```
Byte 0: Якість (1-5)
```

**Значення**:
| Значення | Якість |
|----------|--------|
| 1 | Мінімальна (швидкий друк) |
| 2 | Низька |
| 3 | Середня (рекомендується) |
| 4 | Висока |
| 5 | Максимальна (найкращий результат) |

**Приклад**: Максимальна якість
```
DATA = [0x05]
```

**Response**: Немає

---

### 0xAF - SetEnergy (Встановити енергію)

**Опис**: Встановити рівень теплової енергії для друку

**Data Format**:
```
Byte 0: HIGH byte
Byte 1: LOW byte
Енергія = (HIGH << 8) | LOW
```

**Типові значення**:
| Значення | Опис |
|----------|------|
| 0x0800 | Мінімальна (світлий друк) |
| 0x1000 | Помірна (рекомендується) |
| 0x2000 | Висока |
| 0xFFFF | Максимальна (найтемніший друк) |

**Приклад**: Помірна енергія (0x1000)
```
DATA = [0x10, 0x00]  // 0x1000
```

**Response**: Немає

---

### 0xBE - DrawingMode (Режим малювання)

**Опис**: Встановити режим: зображення або текст

**Data Format**:
```
Byte 0: Mode
  0x00 = Image Mode (растрові зображення)
  0x01 = Text Mode (текстовий режим)
```

**Приклад**: Image Mode
```
DATA = [0x00]
```

**Response**: Немає

## Приклади пакетів

### Приклад 1: GetStatus

```
Команда: GetStatus
CMD: 0xA3
DATA: (немає)
LEN: 0x00
CRC8: 0x00

Пакет:
51 78 A3 00 00 00 00 FF
```

### Приклад 2: FeedPaper 30 кроків

```
Команда: FeedPaper
CMD: 0xA1
DATA: [0x00, 0x1E]
LEN: 0x02
CRC8: CRC8([0x00, 0x1E]) = 0x5A

Пакет:
51 78 A1 00 02 00 00 1E 5A FF
     │  │  │  │  │  │  │  │  └─ 0xFF (конец)
     │  │  │  │  │  │  │  └─────── CRC8
     │  │  │  │  │  │  └────────── 0x1E (30)
     │  │  │  │  │  └───────────── 0x00 (HIGH)
     │  │  │  │  └──────────────── резерв
     │  │  │  └─────────────────── 0x02 (LEN)
     │  │  └────────────────────── резерв
     │  └───────────────────────── 0xA1 (CMD)
     └──────────────────────────── 0x78 (Magic)
```

### Приклад 3: SetEnergy помірна

```
Команда: SetEnergy
CMD: 0xAF
DATA: [0x10, 0x00]
LEN: 0x02
CRC8: CRC8([0x10, 0x00]) = 0x57

Пакет:
51 78 AF 00 02 00 10 00 57 FF
```

## Послідовність ініціалізації

Примітний порядок команд при запуску:

```
1. SetEnergy [0x10, 0x00]      → помірна енергія
   delay(300ms)

2. SetQuality [0x05]           → максимальна якість
   delay(300ms)

3. DrawingMode [0x00]          → режим зображення
   delay(300ms)

4. Готово до використання
```

## Часування

### Критичні затримки

- **Між ініціалізаційними командами**: 300ms
- **Між DrawBitmap + FeedPaper**: 40-50ms
- **Після запиту GetStatus**: 100-200ms
- **На початку/кінці сесії**: 500-1000ms

### Примітка

Якщо затримки занадто короткі, принтер може втратити дані або розірвати BLE з'єднання.

## BLE Конфігурація

### Параметри з'єднання

- **Connection Interval**: 20-100ms (рекомендується 40ms)
- **Slave Latency**: 0 (без затримки)
- **Supervision Timeout**: 4000ms

### MTU (Maximum Transmission Unit)

- **Default**: 23 байти
- **Можна збільшити**: до 517 байт (опціонально)

## Помилки та діагностика

### Проблема: BLE розривається

**Причини**:
- Папір застряв
- Дроїд живлення занизькі
- Перевтома WiFi чипу

**Рішення**:
- Перевір рівень батареї
- Дай хоча б 100ms затримки між командами
- Перезавантаж принтер

### Проблема: Принтер не друкує

**Причини**:
- Неправильний режим малювання
- Неправильне форматування зображення
- Енергія встановлена занизько

**Рішення**:
- Переконайтесь DrawingMode = 0x00
- Перевірте формат растру (1bpp)
- Збільште енергію

---

**Послідня редакція**: 09.06.2026

**Джерела**:
- WerWolv/PythonCatPrinter (оригінальний реверс-інжинірінг)
- Тестування на ESP32-C3
